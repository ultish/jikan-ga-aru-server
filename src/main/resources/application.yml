spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: jikan-ga-aru
      auto-index-creation: true


logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG